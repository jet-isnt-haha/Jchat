import styles from '../pages/index.module.less';
const HistoryFooter = () => {
	return (
		<footer className={styles.footer}>
			<span className={styles.userName}>jet chen</span>
			<span className={styles.settingsIcon} />
		</footer>
	);
};

export default HistoryFooter;
